# Selecting Random People from a Group of People,
# who will going to pay the Restaurant Bills Today!

import random

group_of_people = ["Mamun", "Sadi", "Shahin", "Shuvo", "Kamrul"]
length_of_list = len(group_of_people)
#print(len_of_list)

random_number = random.randint(0,(length_of_list-1))
print(random_number)
print(f'Today','*',group_of_people[random_number],'*','will pay the Bills')
